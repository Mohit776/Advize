rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read, write access for all users for now
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
